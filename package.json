{
  "name": "app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "mocha --opts test/mocha.opts src/**/__specs__/*.spec.js",
    "test_watch": "mocha --watch --opts test/mocha.opts src/**/__specs__/*.spec.js"
  },
  "dependencies": {
    "react-native": "^0.20.0",
    "redux": "^3.3.1",
    "redux-saga": "^0.9.1"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-polyfill": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "expect": "^1.14.0",
    "install": "^0.5.0",
    "isomorphic-fetch": "^2.2.1",
    "mocha": "^2.4.5"
  },
  "babel": {
    "presets": [
      "react-native"
    ]
  }
}
