import { StyleSheet } from 'react-native';

export const MAX_HEIGHT = 32;

export default StyleSheet.create({
  charCount: {
    fontSize: 14,
    color: '#c0cbcc',
    textAlign: 'right',
    margin: 28,
    marginTop: -20,
    marginBottom: 0,
  },
});
