import { StyleSheet } from 'react-native';
import colors from 'src/constants/colors';

export default StyleSheet.create({
  hint: {
    textAlign: 'left',
    marginLeft: 24,
    marginRight: 24,
    color: colors.TEXT_INPUT_HINT,
    fontSize: 14,
  },
});
